/**
 * Light "bone" theme. The backdrop is a soft warm paper with a gentle vignette
 * rather than flat cream, so the frame still has depth.
 *
 * GOLD is a deepened version of the cap's own embroidery gold (sampled at
 * ~#a58556) — the literal thread colour is too light to hold contrast against
 * paper, so it is darkened for type while staying in the same family.
 */
export const PAGE_BG =
  "radial-gradient(ellipse 120% 75% at 50% 42%, #faf7f1 0%, #f1eade 55%, #e3dac9 100%)";

export const INK = "#16130f";
export const INK_SOFT = "#6f665a";
export const GOLD = "#8a6a33";
export const CREAM = "#faf7f1";
/** Tonal wordmark ticker — sits just off the paper, like the site's grey-on-black hero. */
export const MARQUEE_INK = "#c9beab";
