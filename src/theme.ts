/**
 * Backdrop for every section. A soft warm-graphite spotlight rather than flat
 * black — it keeps the brand's dark identity but gives the frame depth so it
 * reads as a lit studio wall instead of an empty void.
 */
export const PAGE_BG =
  "radial-gradient(ellipse 120% 75% at 50% 42%, #2e2721 0%, #16130f 55%, #0b0a09 100%)";
